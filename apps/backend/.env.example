PORT=3000
DATABASE_URL=postgresql://postgres:replace-with-a-strong-database-password@localhost:5432/solidarity?schema=public
CORS_ORIGIN=http://localhost:4200,https://solidarity-network.rhuanpasti.workers.dev
JWT_SECRET=replace-with-a-random-secret-with-at-least-32-characters
# Optional seed credentials for local/bootstrap environments only.
# Leave empty to avoid creating a default login.
# SEED_ADMIN_EMAIL=operations@example.com
# SEED_ADMIN_USERNAME=admin
# SEED_ADMIN_PASSWORD=replace-with-a-strong-password
